Datatypes
---------

#. ``C`` instructions can be broadly classified into ``compile time`` &
   ``run time`` instructions. Check the example, `sizeof.c` in ``pwd`` ::

.. literalinclude:: sizeof.c
	:linenos:
	:language: c

#. This is next
